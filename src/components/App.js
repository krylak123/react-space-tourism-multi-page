import React from 'react';
import { HashRouter as Router } from 'react-router-dom';

const App = () => {
  console.log('work');

  return (
    <Router>
      <div>
        <p>React App</p>
      </div>
    </Router>
  );
};

export default App;
